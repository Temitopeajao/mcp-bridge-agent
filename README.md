# mcp-bridge-agent
A modular AI orchestrator built with p-agent and Google's Gemma 2. This project implements a local-first architecture using the Model Context Protocol (MCP) to securely synthesize data from SQL databases and local filesystems without cloud dependency.
